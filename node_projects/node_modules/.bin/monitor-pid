#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../monitor-pid/bin/monitor-pid" "$@"
  ret=$?
else 
  node  "$basedir/../monitor-pid/bin/monitor-pid" "$@"
  ret=$?
fi
exit $ret
